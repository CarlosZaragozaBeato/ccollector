# Base de datos en memoria para tests — sin PostgreSQL externo
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.liquibase.enabled=false

# Deshabilitar retries en tests para evitar delays artificiales
Retry/enabled=false

# REST client URL para tests (no se hace llamadas reales)
quarkus.rest-client.strava-api.url=http://localhost:8089

# Config Strava OAuth — valores dummy para que el contexto CDI arranque
strava.client-id=test-client-id
strava.client-secret=test-client-secret

# Encryption key para tests (AES-256-GCM)
collector.encryption.key=UYVtd7hspZcd6gSmPxYJ+sl//4ivnUmGJHGT97ir4f4=

# Logging reducido en tests
quarkus.log.level=WARN
quarkus.log.category."com.zensyra".level=INFO
quarkus.log.console.json.enabled=false
